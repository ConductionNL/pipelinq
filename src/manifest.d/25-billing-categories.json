{
	"menu": [
		{
			"id": "BillingCategories",
			"label": "Billing categories",
			"icon": "icon-toggle",
			"route": "BillingCategories",
			"order": 145
		}
	],
	"pages": [
		{
			"id": "BillingCategories",
			"_note": "Billing-category management as a declarative type:index page (pipelinq-views-to-declarative-r2): name→swatch (colorField:'color'), isDba→DBA boolean badge, isActive→active badge, isDefault→boolean, defaultSort type-then-name. 'New category' navigates to the bespoke BillingCategoryNew wizard (CnWizardDialog); row open navigates to the declarative BillingCategoryDetail page.",
			"route": "/billing-categories",
			"type": "index",
			"title": "Billing categories",
			"config": {
				"register": "pipelinq",
				"schema": "billingCategory",
				"documentationUrl": "https://pipelinq.conduction.nl",
				"showAdd": false,
				"showFormDialog": false,
				"showEditAction": false,
				"showCopyAction": false,
				"showDeleteAction": false,
				"showMassImport": false,
				"showMassExport": false,
				"showMassCopy": false,
				"showMassDelete": false,
				"defaultSort": [
					{ "field": "type", "order": "asc" },
					{ "field": "name", "order": "asc" }
				],
				"columns": [
					{ "key": "name", "label": "Name", "format": { "style": "swatch", "colorField": "color" } },
					"code",
					"type",
					{ "key": "isDba", "label": "DBA", "type": "boolean" },
					{ "key": "isDefault", "label": "Default", "type": "boolean" },
					{ "key": "isActive", "label": "Active", "type": "boolean" }
				],
				"headerActions": [
					{ "id": "create", "label": "New category", "icon": "Plus", "handler": "navigate", "route": "BillingCategoryNew" }
				],
				"actions": [
					{ "id": "view", "label": "Open", "icon": "Eye", "handler": "navigate", "route": "BillingCategoryDetail" }
				]
			}
		},
		{
			"id": "BillingCategoryNew",
			"route": "/billing-categories/new",
			"type": "custom",
			"title": "New billing category",
			"component": "CnWizardDialog",
			"config": {
				"register": "pipelinq",
				"schema": "billingCategory"
			}
		},
		{
			"id": "BillingCategoryDetail",
			"route": "/billing-categories/:id",
			"type": "detail",
			"title": "Billing category",
			"config": {
				"register": "pipelinq",
				"schema": "billingCategory",
				"documentationUrl": "https://pipelinq.conduction.nl",
				"_note": "Config archetype (bespoke-detail-pages). A billing category is a tiny reference/config row (name, code, type, colour, isDefault, requiresWbsoRef, isDba, isActive) used to tag billable work — nobody communicates with it and it has no documents, so this is deliberately the leanest page in the set: NO comms leaves, NO files, NO object-list. A single 3-column config data widget shows every field at a glance; the related widget alongside surfaces anything that references this category. Kept intentionally minimal so it reads as a settings row, not a workspace.",
				"widgets": [
					{ "id": "billcat-data", "type": "data", "title": "Billing category", "content": { "columns": 3 } },
					{ "id": "billcat-related", "type": "related", "title": "Used by" }
				],
				"layout": [
					{ "id": "1", "widgetId": "billcat-data",    "gridX": 0, "gridY": 0, "gridWidth": 8, "gridHeight": 3 },
					{ "id": "2", "widgetId": "billcat-related", "gridX": 8, "gridY": 0, "gridWidth": 4, "gridHeight": 3 }
				],
				"sidebar": {
					"enabled": true,
					"showMetadata": true,
					"tabs": [
						{ "id": "audit", "label": "History", "component": "CnAuditTrailTab" }
					]
				}
			}
		}
	]
}
