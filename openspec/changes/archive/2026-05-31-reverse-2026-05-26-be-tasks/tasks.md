# Tasks

Reverse-spec annotation of 3 method(s) for task validation and deadline calculation. Each task = one method annotated against the `task-background-jobs` capability (observed behavior).

- [x] task-1: task-background-jobs — `lib/Service/TaskService.php::calculateDeadline` (reverse-spec annotation)
- [x] task-2: task-background-jobs — `lib/Service/TaskService.php::getDefaultDeadline` (reverse-spec annotation)
- [x] task-3: task-background-jobs — `lib/Service/TaskService.php::validateTask` (reverse-spec annotation)
