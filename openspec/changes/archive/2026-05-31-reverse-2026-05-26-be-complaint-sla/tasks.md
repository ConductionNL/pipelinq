# Tasks

Reverse-spec annotation of 3 method(s) for complaint SLA deadline computation. Each task = one method annotated against the `klachtenregistratie` capability (observed behavior).

- [x] task-1: klachtenregistratie — `lib/Service/ComplaintSlaService.php::calculateDeadline` (reverse-spec annotation)
- [x] task-2: klachtenregistratie — `lib/Service/ComplaintSlaService.php::getSlaHoursForCategory` (reverse-spec annotation)
- [x] task-3: klachtenregistratie — `lib/Service/ComplaintSlaService.php::isOverdue` (reverse-spec annotation)
