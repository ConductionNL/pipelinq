# Tasks

Reverse-spec annotation of 1 method(s) for prospect discovery cards. Each task = one method annotated against the `prospect-discovery` capability (observed behavior).

- [x] task-1: prospect-discovery — `src/components/ProspectCard.vue::scoreClass` (reverse-spec annotation)
