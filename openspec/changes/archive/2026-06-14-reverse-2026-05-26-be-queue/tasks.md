# Tasks

Reverse-spec annotation of 3 method(s) for queue depth, capacity and overflow handling. Each task = one method annotated against the `queue-management` capability (observed behavior).

- [x] task-1: queue-management — `lib/Service/QueueService.php::getQueueDepth` (reverse-spec annotation)
- [x] task-2: queue-management — `lib/Service/QueueService.php::isAtCapacity` (reverse-spec annotation)
- [x] task-3: queue-management — `lib/Service/QueueService.php::processOverflow` (reverse-spec annotation)
