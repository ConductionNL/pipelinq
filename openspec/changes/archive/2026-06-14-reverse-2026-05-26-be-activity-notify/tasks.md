# Tasks

Reverse-spec annotation of 3 method(s) for activity publishing and notification delivery. Each task = one method annotated against the `notifications-activity` capability (observed behavior).

- [x] task-1: notifications-activity — `lib/Service/ActivityService.php::publishDealLost` (reverse-spec annotation)
- [x] task-2: notifications-activity — `lib/Service/NotificationService.php::notifyDealLost` (reverse-spec annotation)
- [x] task-3: notifications-activity — `lib/Service/NotificationService.php::sendNotification` (reverse-spec annotation)
