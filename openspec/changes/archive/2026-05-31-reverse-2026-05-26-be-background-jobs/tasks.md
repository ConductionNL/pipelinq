# Tasks

Reverse-spec annotation of 2 method(s) for scheduled CRM background jobs. Each task = one method annotated against the `task-background-jobs` capability (observed behavior).

- [x] task-1: task-background-jobs — `lib/BackgroundJob/CallbackOverdueJob.php::run` (reverse-spec annotation)
- [x] task-2: task-background-jobs — `lib/BackgroundJob/QueueOverflowJob.php::run` (reverse-spec annotation)
