# Tasks

Reverse-spec annotation of 4 method(s) for contact synchronisation screens. Each task = one method annotated against the `contacts-sync` capability (observed behavior).

- [x] task-1: contacts-sync — `src/views/sync/SyncSettings.vue::loadSettings` (reverse-spec annotation)
- [x] task-2: contacts-sync — `src/views/sync/SyncSettings.vue::updateAccounts` (reverse-spec annotation)
- [x] task-3: contacts-sync — `src/views/sync/SyncSettings.vue::updateCalendarSync` (reverse-spec annotation)
- [x] task-4: contacts-sync — `src/views/sync/SyncSettings.vue::updateSyncEnabled` (reverse-spec annotation)
