{
  "_meta": {
    "spdx-license": "EUPL-1.2",
    "spdx-copyright": "2026 Conduction B.V.",
    "description": "Somewhere to put an UNVERIFIED MFA secret (ADR-037 fragment). Additive only: one new optional property, nothing changes type and nothing becomes required, so the register import needs no breaking-change acknowledgement.\n\nWHY IT HAS TO EXIST. PortalAuthController::mfaEnroll() said, in its own comment, 'stash the encrypted secret as pending until verification confirms it' -- and then wrote it to `mfaSecret`, the LIVE field, while setting `mfaEnabled = false`. There was nowhere else to put it. The effect was that calling enroll on an account with a working second factor destroyed that factor and switched MFA off, before proving anything: anyone holding a stolen password could strip the second factor and then log in with the password alone.\n\nThe secret is stored encrypted, exactly like `mfaSecret`, and is cleared on successful verification."
  },
  "components": {
    "schemas": {
      "portalAccount": {
        "properties": {
          "mfaPendingSecret": {
            "type": "string",
            "title": "Pending MFA secret",
            "description": "An enrolled-but-not-yet-verified TOTP secret, encrypted at rest. Written by mfaEnroll and promoted to `mfaSecret` by mfaVerify once a valid code proves the authenticator holds it; cleared on success. Never a fallback for authentication -- a code is only accepted against this field DURING enrolment, never for a login, or the pending value would itself become a second factor nobody verified."
          }
        }
      }
    }
  }
}
