name: Issue Triage

on:
  issues:
    types: [opened, labeled]
  workflow_dispatch:
    inputs:
      backlog-existing:
        description: "Triage all existing untriaged open issues"
        type: boolean
        default: true

jobs:
  triage:
    uses: ConductionNL/.github/.github/workflows/issue-triage.yml@ed13d49247738543a6cd0b6da4c404273c837566
    with:
      app-name: pipelinq
      backlog-existing: ${{ github.event_name == 'workflow_dispatch' && inputs.backlog-existing || false }}
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
