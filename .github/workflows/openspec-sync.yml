name: OpenSpec Sync

on:
  push:
    branches: [development]
    paths: ['openspec/**']
  workflow_dispatch:

jobs:
  sync:
    uses: ConductionNL/.github/.github/workflows/openspec-sync.yml@ed13d49247738543a6cd0b6da4c404273c837566
    with:
      app-name: pipelinq
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
